# 다함장묘 홈페이지 배포본

정적 웹사이트입니다. GitHub Pages에서 별도 빌드 없이 바로 배포할 수 있습니다.

- 시작 파일: `index.html`
- GitHub Pages: repository Settings → Pages → Deploy from a branch → `main` / `(root)`
- `.nojekyll` 포함
- 전화/문자 버튼은 모바일에서 바로 연결
- 공개 주소 확정 후 `og:image`, canonical, sitemap 절대 URL을 최종 반영 권장
